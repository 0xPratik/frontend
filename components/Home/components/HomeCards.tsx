import { Heading, VStack } from '@chakra-ui/react'
import React from 'react'

type Props = {}

const HomeCards = (props: Props) => {
    return (
      <VStack>
        <Heading>Super packed with features just for you</Heading>
      </VStack>
    );
}

export default HomeCards